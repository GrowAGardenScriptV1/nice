local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- UI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TapToLoadUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Black background
local bg = Instance.new("Frame")
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
bg.Parent = screenGui

-- Red transparent overlay
local overlay = Instance.new("Frame")
overlay.Size = UDim2.new(1, 0, 1, 0)
overlay.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
overlay.BackgroundTransparency = 0.8
overlay.ZIndex = 1
overlay.Parent = bg

-- TAP button (invisible full screen)
local tapButton = Instance.new("TextButton")
tapButton.Size = UDim2.new(1, 0, 1, 0)
tapButton.BackgroundTransparency = 1
tapButton.Text = ""
tapButton.ZIndex = 2
tapButton.Parent = bg

-- TAP TO LOAD label
local tapText = Instance.new("TextLabel")
tapText.Size = UDim2.new(0, 300, 0, 60)
tapText.Position = UDim2.new(0.5, -150, 0.5, -100)
tapText.Text = "TAP TO LOAD"
tapText.TextScaled = true
tapText.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
tapText.TextColor3 = Color3.fromRGB(255, 255, 255)
tapText.Font = Enum.Font.GothamBold
tapText.ZIndex = 2
tapText.Parent = bg

-- Loading bar (centered)
local bar = Instance.new("Frame")
bar.Size = UDim2.new(0, 0, 0, 10)
bar.Position = UDim2.new(0.5, -150, 0.5, 0)
bar.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
bar.BorderSizePixel = 0
bar.ZIndex = 2
bar.Parent = bg

-- Percent text
local percentText = Instance.new("TextLabel")
percentText.Size = UDim2.new(0, 200, 0, 50)
percentText.Position = UDim2.new(0.5, -100, 0.5, 20)
percentText.Text = "0%"
percentText.TextScaled = true
percentText.BackgroundTransparency = 1
percentText.TextColor3 = Color3.fromRGB(255, 255, 255)
percentText.Font = Enum.Font.GothamBold
percentText.ZIndex = 2
percentText.Parent = bg

-- Wait text
local waitText = Instance.new("TextLabel")
waitText.Size = UDim2.new(0, 400, 0, 60)
waitText.Position = UDim2.new(0.5, -200, 0.5, 50)
waitText.Text = "Please wait 5 minutes..."
waitText.TextScaled = true
waitText.BackgroundTransparency = 1
waitText.TextColor3 = Color3.fromRGB(255, 255, 255)
waitText.Font = Enum.Font.GothamBlack
waitText.Visible = false
waitText.ZIndex = 2
waitText.Parent = bg

-- GELOHUB label
local geloHub = Instance.new("TextLabel")
geloHub.Size = UDim2.new(0, 300, 0, 60)
geloHub.Position = UDim2.new(0.5, -150, 0, 20)
geloHub.Text = "GELOHUB"
geloHub.TextScaled = true
geloHub.BackgroundTransparency = 1
geloHub.TextColor3 = Color3.fromRGB(255, 0, 0)
geloHub.Font = Enum.Font.GothamBlack
geloHub.ZIndex = 2
geloHub.Parent = bg

-- Load logic
local percent = 0
local loading = false

tapButton.MouseButton1Click:Connect(function()
	if loading then return end

	if percent < 100 then
		percent += 2
		bar.Size = UDim2.new(percent / 100, 0, 0, 10)
		percentText.Text = percent .. "%"
	end

	if percent >= 100 and not loading then
		loading = true
		tapButton.Visible = false
		tapText.Visible = false
		waitText.Visible = true

		-- Wait 5 minutes before removing UI
		task.delay(300, function()
			screenGui:Destroy()
		end)
	end
end)
